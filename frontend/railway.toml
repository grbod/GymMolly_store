[build]
builder = "nixpacks"
buildCommand = "DISABLE_ESLINT_PLUGIN=true npm install --legacy-peer-deps && CI=false npm run build"

[deploy]
startCommand = "npm run build && npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[deploy.env]
NODE_ENV = "production"
PORT = "3000" 